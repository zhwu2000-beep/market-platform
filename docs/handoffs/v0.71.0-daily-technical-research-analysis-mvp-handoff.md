# v0.71.0 Daily Technical Research Analysis MVP Handoff

## Status

READY FOR FINAL REVIEW

The implementation remains uncommitted and unstaged on
`feature/v0.71.0-daily-technical-research-analysis-mvp` at base
`106ff9d8241976e1fa9b01a29e58b10cba285f74`; package version remains 0.1.0.

## Delivered contract

Polygon daily acquisition explicitly requests adjusted ascending history with
the 50,000-result cap and requires exact adjusted-response proof. Daily raw UTC
instants normalize through their New York aggregate date to midnight UTC;
intraday behavior is untouched. The workflow explicitly selects Polygon with
no fallback and uses the fixed 450-calendar-day policy.

The additive request/result/workflow connect acquisition to released v0.70
completed evidence and analysis. The result owns only request and snapshot.
Research exports total 77 with the released 74 as an exact prefix; indicator
exports remain six and schema/fingerprint families remain three.

The discovered configuration-error contract conflict was resolved by accepting
the existing service boundary: direct Polygon missing credentials remain
`ConfigurationError`, while `MarketDataService` may normalize the failure to
`DataProviderError`. `data/service.py` is unchanged, and the CLI returns 1 for
either acquisition failure.

`research analyze` accepts one venue-qualified instrument, daily/Polygon only,
an optional aware analysis time, table or JSON rendering, and optional file
output. The CLI is the sole clock boundary. Table formatting is presentation
only; JSON and fingerprints retain semantic values. Outputs are non-actionable
and add no signal, risk, broker, or execution behavior.

## Authorized inventory

Exactly 14 paths changed: nine modified and five new, with zero deleted and zero
renamed. Final inventory-aware totals are 2,306 insertions and 27 deletions. No
change exists in `data/service.py` or any other unauthorized path.

## Consolidated required-fix pass

Two independent read-only reviews inspected the same pre-fix implementation.
Review A reported 1 blocker, 6 required, 2 recommended, and 1 nit. Review B
reported 1 blocker, 4 required, 2 recommended, and 0 nits. Findings overlapped;
the consolidated union was 1 blocker and 9 required findings, all repaired in
place.

Adjusted evidence is now bound to the exact production Polygon route and exact
adjusted response proof. Permanent regressions cover aware datetime, strict
CLI grammar, pre-I/O validation, factory exit mapping, result/table validity,
duplicates, retained instrument integrity, malformed Polygon values, negative
zero/tiny display, and structural 41-row ordering. Final command results and
the actual mypy checked-source count are recorded after final validation.


The post-fix implementation is ready for final review, not released.

## Validation

- all permanent provider/application/CLI regressions passed;
- v0.70 focused regressions: 108 passed;
- combined focused and broad historical compatibility passed;
- complete pytest: 3,374 passed, 2 skipped;
- optional real Polygon smoke: skipped by default; opt-in/credentials unavailable;
- Ruff: all checks passed;
- mypy: no issues in 178 source files;
- source Python files: 178;
- indicator exports: 6;
- research exports: 77, with the released 74 as the exact prefix;
- schema/fingerprint families: exactly the three released v0.70 families;
- AAPL/NASDAQ identity:
  `sha256:dc586683e7966f5f6a9060934d37a28a594fe22b6cd42f40b5f5228e13cba433`;
- Replay schema: `1.1.0`;
- Replay 100: `sha256:164d6fde9ea66ff877bd2adb5c45758c5dc9acf05da5380ef95ae9823388b420`;
- Replay 300: `sha256:f53811a027cdf5a729502f2defb985fa7d6d79c22867e3d4f9be9e5d8dfa14ed`;
- Replay 500: `sha256:01aa5a29aeb79366febc6f6f29adec0ffb7ddafa9a78a947336ac275c98e4e03`;
- deterministic injected NVDA table and JSON acceptance: passed;
- `git diff --check`: passed.

The package remains 0.1.0. Repository caches, bytecode, temporary artifacts,
external validation artifacts, and reparse points are zero after cleanup.
Released and protected refs remain exact; the index and stash are empty and no
Git operation is active.
