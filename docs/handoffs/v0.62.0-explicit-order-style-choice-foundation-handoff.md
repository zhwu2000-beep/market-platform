# v0.62.0 Explicit Order Style Choice Foundation Handoff

## Release State

Implementation is uncommitted on `feature/v0.62.0-explicit-order-style-choice`, based on released v0.61.0 target `c400efb3810451ecedf73d5bd59aa1365b1c2b61`. Package version remains `0.1.0`.

## Public API

`market_platform.execution_planning` preserves all twelve v0.61 exports and adds exactly:

- `ORDER_STYLE_CHOICE_SCHEMA`;
- `OrderStyle`;
- `OrderStyleChoice`.

The final package inventory contains exactly fifteen exports.

## Domain Contract

`OrderStyle` contains exactly `MARKET = "market"` and `LIMIT = "limit"`. `OrderStyleChoice(style)` requires an exact enum instance; missing values, strings, foreign enums, `None`, and arbitrary objects are rejected. Absence never means market.

The frozen/slotted caller-authored value exposes only style, derived schema, and derived fingerprint. Its exact JSON-safe projection contains `schema_version`, `style`, and `fingerprint`. Schema and fingerprint are not caller-supplied.

## Correspondence and Threat Boundary

Projection safely retrieves all three public slots and rejects wrong retained style type, stale schema, stale fingerprint, and deleted state with `ExecutionPlanningCorrespondenceError`. Unexpected programming exceptions propagate.

This is not a factory-derived result. It has no token, constructor-state tuple, identity binding, retained instruction, or provenance claim. A caller may directly construct either valid choice. The fingerprint is deterministic identity, not cryptographic authenticity.

## Scope

MARKET is only a label, not immediate execution or authorization. LIMIT intentionally has no price. V0.62 adds no instruction coupling, time, price, TIF, session, position effect, account, instrument, broker capability, order specification, authorization, submission, persistence, or lifecycle semantics.

## Validation

Validation passed: Ruff; mypy across 168 source files; 51 focused v0.62 tests; both corrected exact package-API inventory tests; 1,006 focused v0.55-v0.62 compatibility tests; the complete reconciled 648-test wider compatibility selection; and 2,544 passed with one established skip repository-wide. Historical Replay schema 1.1.0, Replay fingerprints, and the released AAPL/NASDAQ identity remain exact. The implementation changes exactly ten paths and remains unstaged, uncommitted, cache-free, and READY FOR REVIEW with package version `0.1.0`. All original twelve exports remain, the three approved v0.62 additions produce exactly fifteen exports, and execution planning contains exactly three fingerprint families.
